(function ($) {'use strict'; 	$(document).ready(function () {	  //Default Action	  $(".tab_content").hide(); //Hide all content	  $("ul.tabs li:first").addClass("active").fadeIn(500); //Activate first tab	  $(".tab_content:first").fadeIn(500); //Show first tab content	  //On Click Event	  $("ul.tabs li").on("click", function () {	    $("ul.tabs li").removeClass("active"); //Remove any "active" class	    $(this).addClass("active"); //Add "active" class to selected tab	    $(".tab_content").hide(); //Hide all tab content	    var activeTab = $(this).find("a").attr("href"); //Find the rel attribute value to identify the active tab + content	    $(activeTab).fadeIn(500); //Fade in the active content	    return false;	  });	  $("#evrplus-donate-box").fadeIn();	  $("img.evrplus-close").on("click", function () {	    $("#evrplus-donate-box").fadeOut();	  });	  	});}(jQuery));